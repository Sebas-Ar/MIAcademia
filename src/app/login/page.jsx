import LoginPage from '@/frontend/components/login/LoginPage'

export const metadata = {
    title: 'Login | MiAcademia',
    robots: {
        index: false,
        follow: false
    }
}

const Page = () => {
    return <LoginPage />
}

export default Page
